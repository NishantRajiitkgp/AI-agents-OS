---
description: Run every registered gate and report what each returned.
---

Runs every gate in `aios/gates.yml` and reports what each returned.

```
python .github/scripts/run-gates.py $ARGUMENTS
```
