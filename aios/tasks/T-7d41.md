---
id: T-7d41
title: Turn on branch protection so CODEOWNERS enforces anything
status: todo
satisfies: [GUARD-1, GUARD-2]
priority: 1
risk: high
blocked_by: []
touches:
  - .github/CODEOWNERS
  - docs/runbooks/
acceptance:
  - "When a pull request modifies a protected path, the system shall require a review from a named owner before the merge is available"
  - "The system shall refuse a force push and a history rewrite on the default branch"
  - "If the branch settings are changed, then the system shall leave a record of what they were, rather than existing only as the current state of a web form"
verify:
  - python3 .github/scripts/check-codeowners.py
  - python3 -m unittest discover -s tests
constraints:
  - "This is the only one of the three containment layers that is enforced server-side. The pre-commit trailer and the tool deny lists run on the machine being controlled and are convenience (GUARD-2)."
  - "Do not weaken the protected set to make the settings easier. The set is `protected_paths` in aios/config.yml and check-codeowners.py exists to catch the two drifting apart."
---

## Context

Carried over from `M2-01` and `M2-02`, which were one piece of work described as two.
`.github/CODEOWNERS` is written, covers every entry in `protected_paths`, and now names a
resolvable handle rather than the placeholder — but a CODEOWNERS file changes nothing at all
until "Require review from Code Owners" is switched on for the default branch.

That gap is the reason `check-codeowners.py` prints what it cannot check instead of implying
otherwise. GitHub does not reject an owner it cannot resolve: it drops the rule and reports
the file as valid, so a CODEOWNERS naming nobody is indistinguishable from one naming
somebody. The same shape of failure applies one level up — a covered file list with no branch
protection behind it reads as a control that works right up until it is tested.

`GUARD-1` is the containment requirement everything else in the gate layer rests on, and
`GUARD-2` is why this task cannot be satisfied by anything committed to the repository. The
settings live with the forge; the repository can only record them.

## Outcome

Open. The settings are the deliverable, and the runbook recording them is what makes this a
configuration rather than a thing somebody once clicked.
